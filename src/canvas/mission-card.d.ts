import { type Difficulty } from '../utils/constants.js';
export declare function generateMissionCard(heistName: string, difficulty: Difficulty, submitter: string, teammates: string[], xpAwarded: number, coinsAwarded: number, approved: boolean): Promise<Buffer>;
//# sourceMappingURL=mission-card.d.ts.map